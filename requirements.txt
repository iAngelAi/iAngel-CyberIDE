# Backend & CLI Dependencies for CyberIDE (Local Hosting)
# Usage: pip install -r requirements.txt

# Core API & Server (Pour le pont Local <-> UI)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# CLI & Terminal Visuals (Pour le style Geek dans le terminal)
typer>=0.9.0
rich>=13.6.0
colorama>=0.4.6

# File Watching & Analysis (Pour détecter les changements et MAJ le cerveau)
watchdog>=3.0.0
pyyaml>=6.0.1

# Testing & Quality (Critères de réussite pour l'illumination)
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-json-report>=1.5.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
black>=23.9.1
pylint>=3.0.0

# AI Integration (Optionnel pour le futur MCP)
openai>=1.3.0
anthropic>=0.5.0

# Vector Database & Embeddings (Pour la mémoire vectorielle RAG)
chromadb>=0.4.0
sentence-transformers>=2.2.0